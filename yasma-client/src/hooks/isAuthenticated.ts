import { useState, useEffect } from "react";
import {isAuthenticated} from "@/services/api/api";

const isAuthenticated = () => {

  useEffect(() => {
    sessionStorage.getItem(name);
  }, [])

  return value
}

export default isAuthenticated
